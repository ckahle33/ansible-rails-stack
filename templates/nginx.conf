server {
  listen 80;
  server_name {{ server_name }};
  root /home/{{ project_name }}/current/public;
  access_log /home/{{ project_name }}/current/log/nginx_access.log;
  error_log /home/{{ project_name }}/current/log/nginx_error.log;
  passenger_enabled on;
  rack_env production;
}
